# GEO-Pulse 개발 작업 계획 (MVP - 비로그인)

## 완료된 작업 ✅

### 0. 프로젝트 초기 설정
- [x] Next.js 16 + TypeScript + App Router 설정
- [x] Tailwind CSS 4 + shadcn/ui 설정
- [x] Prisma + PostgreSQL 스키마 정의
- [x] Docker Compose (PostgreSQL, Redis) 설정
- [x] Redis 클라이언트 및 BullMQ 큐 설정
- [x] 환경변수 관리 체계 구축 (@t3-oss/env-nextjs)
- [ ] DB 마이그레이션 실행

---

## Phase 1: MVP 핵심 화면

### 1.1 🏠 랜딩 페이지 (`/`)
> 서비스 소개 및 URL 분석 시작

**UI 컴포넌트**
- [ ] 히어로 섹션 (서비스 소개, CTA 버튼)
- [ ] URL 입력 폼
- [ ] 기능 소개 섹션
- [ ] 최근 분석 목록 (로컬스토리지 기반)
- [ ] 푸터

**라우트:** `/`

---

### 1.2 ⏳ 분석 진행 화면 (`/analysis/[id]/progress`)
> 분석 진행 상태 실시간 표시

**UI 컴포넌트**
- [ ] 분석 대상 URL 표시
- [ ] 진행 상태 표시 (스피너, 프로그레스 바)
- [ ] 현재 분석 단계 표시 (크롤링 → SEO 분석 → GEO 분석)
- [ ] 예상 소요 시간
- [ ] 취소 버튼

**API**
- [ ] `POST /api/analysis` - 새 분석 생성
- [ ] `GET /api/analysis/[id]/status` - 분석 상태 조회 (폴링)

**백엔드**
- [ ] 분석 작업 생성 및 큐 등록
- [ ] 분석 워커 프로세스
- [ ] Playwright 웹 크롤러
- [ ] SEO 분석 엔진
  - [ ] Meta 태그 분석 (title, description, keywords)
  - [ ] Header 구조 분석 (H1~H6)
  - [ ] 스키마 마크업 검출
  - [ ] 페이지 성능 측정
- [ ] GEO 분석 엔진 (OpenAI API)
  - [ ] 인용 신뢰도 분석
  - [ ] LLM 가독성 분석
  - [ ] 엔티티 최적화 분석
- [ ] 상태 업데이트 로직

**라우트:** `/analysis/[id]/progress`

---

### 1.3 📈 분석 결과 상세 (`/analysis/[id]`)
> SEO/GEO 분석 결과 및 점수 표시

**UI 컴포넌트**
- [ ] 분석 대상 URL 및 스크린샷
- [ ] 총점 게이지 바
- [ ] SEO/GEO 점수 레이더 차트
- [ ] SEO 분석 결과 카드
  - [ ] Meta 태그 분석 결과
  - [ ] Header 구조 분석 결과
  - [ ] 스키마 마크업 분석 결과
  - [ ] 성능 분석 결과
- [ ] GEO 분석 결과 카드
  - [ ] 인용 신뢰도 점수
  - [ ] LLM 가독성 점수
  - [ ] 엔티티 최적화 점수
- [ ] 실행 가이드 탭
  - [ ] 마케터용 가이드
  - [ ] 개발자용 가이드
- [ ] 공유 버튼
- [ ] PDF 다운로드 버튼
- [ ] 새 분석 시작 버튼

**API**
- [ ] `GET /api/analysis/[id]` - 분석 결과 조회
- [ ] `POST /api/analysis/[id]/share` - 공유 토큰 생성
- [ ] `GET /api/analysis/[id]/pdf` - PDF 다운로드

**백엔드**
- [ ] 실행 가이드 생성 (OpenAI)
- [ ] 공유 토큰 생성
- [ ] Playwright PDF 생성

**라우트:** `/analysis/[id]`

---

### 1.4 🔗 공유 리포트 (`/report/[shareToken]`)
> 공개 리포트 페이지

**UI 컴포넌트**
- [ ] 분석 결과 요약 (읽기 전용)
- [ ] 총점 및 주요 지표
- [ ] 서비스 소개 CTA

**API**
- [ ] `GET /api/report/[shareToken]` - 공유 리포트 조회

**라우트:** `/report/[shareToken]`

---

## Phase 2: 부가 기능

### 2.1 ⚔️ 경쟁사 비교 (`/compare`)
> 두 사이트 1:1 비교 분석

**UI 컴포넌트**
- [ ] 비교 대상 URL 입력 (2개)
- [ ] 나란히 비교 레이아웃
- [ ] 비교 차트 (레이더, 바 차트)
- [ ] 항목별 승패 표시

**API**
- [ ] `POST /api/compare` - 비교 분석 생성
- [ ] `GET /api/compare/[id]` - 비교 결과 조회

**라우트:** `/compare`, `/compare/[id]`

---

## Phase 3: 수익화 (MVP 이후)

### 3.1 💳 결제/플랜
- [ ] Freemium 제한 로직 (1회 무료)
- [ ] 결제 연동
- [ ] 플랜 페이지

### 3.2 📧 이메일 알림
- [ ] Resend 연동
- [ ] 분석 완료 알림

### 3.3 🔐 로그인/회원가입
- [ ] 인증 시스템
- [ ] 분석 히스토리 저장

---

## 파일 구조 (MVP)

```
src/
├── app/
│   ├── page.tsx                    # 랜딩 페이지
│   ├── analysis/
│   │   └── [id]/
│   │       ├── page.tsx            # 분석 결과
│   │       └── progress/
│   │           └── page.tsx        # 분석 진행
│   ├── report/
│   │   └── [shareToken]/
│   │       └── page.tsx            # 공유 리포트
│   ├── compare/
│   │   ├── page.tsx                # 비교 입력
│   │   └── [id]/
│   │       └── page.tsx            # 비교 결과
│   └── api/
│       ├── analysis/
│       │   ├── route.ts            # POST: 분석 생성
│       │   └── [id]/
│       │       ├── route.ts        # GET: 결과 조회
│       │       ├── status/
│       │       │   └── route.ts    # GET: 상태 조회
│       │       ├── share/
│       │       │   └── route.ts    # POST: 공유 생성
│       │       └── pdf/
│       │           └── route.ts    # GET: PDF 다운로드
│       ├── report/
│       │   └── [shareToken]/
│       │       └── route.ts        # GET: 공유 리포트
│       └── compare/
│           ├── route.ts            # POST: 비교 생성
│           └── [id]/
│               └── route.ts        # GET: 비교 결과
├── components/
│   ├── ui/                         # shadcn/ui
│   ├── charts/
│   │   ├── radar-chart.tsx         # 레이더 차트
│   │   ├── gauge-bar.tsx           # 게이지 바
│   │   └── score-card.tsx          # 점수 카드
│   ├── forms/
│   │   └── url-input-form.tsx      # URL 입력 폼
│   ├── layout/
│   │   ├── header.tsx
│   │   └── footer.tsx
│   └── analysis/
│       ├── seo-result-card.tsx     # SEO 결과 카드
│       ├── geo-result-card.tsx     # GEO 결과 카드
│       ├── progress-status.tsx     # 진행 상태
│       └── action-guide.tsx        # 실행 가이드
├── lib/
│   ├── prisma.ts
│   ├── redis.ts
│   ├── queue.ts
│   ├── openai.ts
│   └── utils.ts
├── services/
│   ├── analysis/
│   │   ├── crawler.ts              # Playwright 크롤러
│   │   ├── seo-analyzer.ts         # SEO 분석
│   │   └── geo-analyzer.ts         # GEO 분석 (OpenAI)
│   └── pdf-generator.ts            # PDF 생성
├── workers/
│   └── analysis-worker.ts          # 분석 워커
└── types/
    └── index.ts
```

---

## 환경 변수 (MVP)

| 변수명 | 설명 | 필수 |
|--------|------|------|
| `DATABASE_URL` | PostgreSQL 연결 URL | ✅ |
| `REDIS_URL` | Redis 연결 URL | ✅ |
| `OPENAI_API_KEY` | OpenAI API 키 | ✅ |
| `NEXT_PUBLIC_APP_URL` | 앱 공개 URL | ✅ |

---

## MVP 개발 순서 (권장)

1. **1.1 랜딩 페이지** - URL 입력 폼, 기본 레이아웃
2. **1.2 분석 진행 화면** - 워커, 크롤러, 분석 엔진
3. **1.3 분석 결과 상세** - 차트, 결과 카드, 가이드
4. **1.4 공유 리포트** - 공유 기능
5. **2.1 경쟁사 비교** - 비교 기능
