# Database (Docker Compose 기본 설정)
DATABASE_URL="postgresql://geopulse:geopulse123@localhost:5432/geopulse?schema=public"

# Redis (Docker Compose 기본 설정)
REDIS_URL="redis://localhost:6379"

# OpenAI
OPENAI_API_KEY="sk-..."

# Resend (Email) - 선택사항
RESEND_API_KEY="re_..."

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# 환경변수 검증 스킵 (개발용)
# SKIP_ENV_VALIDATION="true"
